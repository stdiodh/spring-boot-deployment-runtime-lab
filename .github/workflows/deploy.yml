name: Deploy to EC2

on:
  workflow_dispatch:
  push:
    branches:
      - 09-answer

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      RELEASE_DIR: /home/${{ secrets.EC2_USERNAME }}/aandi-deployment-runtime-lab
      APP_IMAGE: aandi-deployment-runtime-lab:latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Grant Gradle permission
        run: chmod +x gradlew

      - name: Run tests and build jar
        run: ./gradlew test bootJar

      - name: Prepare release bundle
        run: |
          # TODO 1. build/libs 아래 jar를 release 폴더로 복사하세요.
          # TODO 2. Dockerfile, compose 파일, env 예시도 함께 올릴 수 있게 묶으세요.
          echo "TODO: release bundle을 준비하세요."

      - name: Write SSH key
        run: |
          # TODO 3. GitHub Secrets의 EC2_SSH_KEY를 파일로 복원하세요.
          # TODO 4. 권한을 600으로 맞추고 known_hosts도 준비하세요.
          echo "TODO: SSH key를 준비하세요."

      - name: Upload release bundle
        run: |
          # TODO 5. EC2 release 디렉터리를 만들고 파일을 업로드하세요.
          echo "TODO: release bundle을 EC2로 업로드하세요."

      - name: Deploy on EC2
        run: |
          # TODO 6. EC2에서 .env 파일을 만들고 운영 시크릿을 채우세요.
          # TODO 7. docker build -> docker compose up -d -> docker logs 순서로 배포를 마무리하세요.
          echo "TODO: EC2에서 배포 명령을 실행하세요."
