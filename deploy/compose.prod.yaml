services:
  app:
    # TODO 1. EC2에서 다시 띄울 앱 이미지 이름을 정하세요.
    image: aandi-deployment-runtime-lab:latest
    container_name: aandi-app
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "8080:8080"
    environment:
      # TODO 2. prod profile이 활성화되도록 환경변수를 연결하세요.
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      # TODO 3. 앱이 필요한 운영 값을 컨테이너 환경변수로 넘기세요.
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS}
      MAIL_HOST: ${MAIL_HOST}
      MAIL_PORT: ${MAIL_PORT}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      APP_FRONTEND_URL: ${APP_FRONTEND_URL}
      APP_PASSWORD_RESET_URL: ${APP_PASSWORD_RESET_URL}

  mysql:
    # TODO 4. 운영에서도 MySQL 컨테이너를 함께 올릴지 확인하세요.
    image: mysql:8.4
    container_name: aandi-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      TZ: Asia/Seoul
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -p$$MYSQL_ROOT_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - mysql-data:/var/lib/mysql

  redis:
    # TODO 5. Redis도 같이 띄워야 현재 앱 기능이 깨지지 않습니다.
    image: redis:7.4
    container_name: aandi-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  mysql-data:
