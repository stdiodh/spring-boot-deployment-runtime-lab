# Deprecated Answer Guide

이 문서는 예전 링크 호환을 위해 남겨둔 안내 문서입니다.
`main` 가이드 브랜치는 두 시퀀스가 공유하는 runtime과 registry 기반 CI/CD 계약을 보여 줍니다.
학생용 TODO와 시퀀스별 비교 기준은 각 implementation/answer 브랜치에서 확인합니다.

학생은 오늘 시퀀스의 starter 브랜치에서 실습한 뒤 같은 번호의 answer 브랜치와 비교합니다.

- `09-implementation` -> `09-answer`: Docker/Runtime 흐름
- `10-implementation` -> `10-answer`: CI/CD build, deploy, verify 흐름

각 브랜치의 `README.md`, `docs/theory.md`, `docs/implementation.md`, `docs/checklist.md`를 기준으로 확인합니다.
